# Iniciar el proyecto

npm init -y 
pnpm init 

# Instalar dependencias desarrollo

npm install / npm i
pnpm install

# Instalar las dependencias de produccion

npm install --omit=dev
pnpm install --prod